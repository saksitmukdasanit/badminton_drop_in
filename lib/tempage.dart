import 'dart:async';
import 'package:flutter/material.dart';

class TemPage extends StatefulWidget {
  const TemPage({super.key});

  @override
  TemPageState createState() => TemPageState();
}

class TemPageState extends State<TemPage> {
  late Future<dynamic> futureModel;

  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
